export interface CardProps {
    icon: JSX.Element;
    title: string;
    description?: string;
}

export interface ProjectDetailsType {
    title: string;
    description: string;
    logo: JSX.Element;
    link: string;
}
